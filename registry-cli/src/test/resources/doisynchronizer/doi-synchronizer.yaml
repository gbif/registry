apiRoot: ${registry-cli-it.api.url}
portalurl: ${registry-cli-it.portal.url}
doiPrefix: 10.21373
datacite:
  username: ${registry-cli-it.datacite.user}
  password: ${registry-cli-it.datacite.password}
  api: ${registry-cli-it.datacite.api.base.url}
messaging:
  host: ${registry-cli-it.messaging.hostname}
  port: ${registry-cli-it.messaging.port}
  username: ${registry-cli-it.messaging.username}
  password: ${registry-cli-it.messaging.password}
  virtualHost: /test
