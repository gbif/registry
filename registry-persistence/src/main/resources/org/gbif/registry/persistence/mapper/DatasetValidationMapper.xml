<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.gbif.registry.persistence.mapper.DatasetValidationMapper">

  <insert id="createOrUpdate">
    INSERT INTO validation_report (dataset_key, attempt, report)
    VALUES (#{datasetKey}, #{attempt}, #{report}::jsonb)
      ON CONFLICT (dataset_key, attempt)
    DO UPDATE SET report = EXCLUDED.report, created = now()
  </insert>

  <select id="get" resultType="java.lang.String">
    SELECT report::text
    FROM validation_report
    WHERE dataset_key = #{datasetKey}
      AND attempt = #{attempt}
  </select>

  <select id="getLatest" resultType="java.lang.String">
    SELECT report::text
    FROM validation_report
    WHERE dataset_key = #{datasetKey}
    ORDER BY attempt DESC
      LIMIT 1
  </select>

</mapper>
